---
description: 项目概述、架构分层、AI 编码原则与常用命令
alwaysApply: true
---

# Atelier Furniture — 项目核心

Vue 3 跨境家具独立站 Demo，复刻 Locomotive.ca 滚动视觉体系。10 SPU 电商闭环，含 Locomotive Scroll + GSAP、Commerce 数据层、性能降级与可观测性。

- 主站：`npm run dev` → http://localhost:5173
- 文档：`docs/ARCHITECTURE.md`、`docs/COMPONENTS.md`、`docs/CONTRIBUTING.md`

## 技术栈

Vue 3.5 · Vite 8 · TS 6 · TailwindCSS 4 · Locomotive Scroll v5 · GSAP · Zod · Pinia · Vitest · Vercel

## 架构分层

Views → Components → Composables/Lib → Data/Store。禁止下层引用上层。

| 层 | 路径 |
|----|------|
| Views | `views/<domain>/`（home / catalog / cart / checkout / about） |
| Components | `layout` / `ui` / `product` / `scroll` |
| Composables + Lib | `composables/`、`lib/scroll`、`lib/motion`、`lib/analytics` |
| Data + Store | `data/`、`store/` |

## 路径别名

`@/` → `src/`，始终用绝对导入，避免 `../../`。

## AI 编码原则

**必须**：最小改动 · 遵循分层 · Zod 推导领域类型 · 复用 BaseButton/ScrollReveal/LazyImage · 动效门控 · scroll 生命周期 · 通过 `npm run check`

**禁止**：新 CSS 方案替代 Tailwind · View 直接 fetch · 绕过 Zod · 重复定义领域类型 · 低端强制 WebGL · 提交 `.env` · 未经请求 commit/push · 过度抽象

## 扩展路径

| 任务 | 做法 |
|------|------|
| 新增页面 | `views/<domain>/` + `router/index.ts` |
| 新增商品 | `data/products.ts` + SEO slugs |
| 接 API | `VITE_COMMERCE_PROVIDER=http` |
| 新增组件 | `components/<layer>/` |

## 常用命令

```bash
npm run dev | build | check | lint:fix | test:watch | docs:dev
```

## 关键文件

`useLocomotiveScroll.ts` · `lib/scroll/animation.ts` · `motionCapabilities.ts` · `schemas.ts` · `api.ts` · `store/cart.ts` · `main.css`

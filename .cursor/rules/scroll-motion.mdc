---
description: Locomotive Scroll、GSAP 动效与性能降级
globs: "app/{composables,lib/scroll,lib/motion,components/scroll}/**"
alwaysApply: false
---

# 滚动与动效

> 对标策略见 `docs/RESEARCH.md`：**Lenis 平滑 + css-progress 驱动**，非 `data-scroll-speed` 堆叠。

## 生命周期

- `layouts/default.vue`：`useLocomotiveScroll` init/destroy；**首屏** `onMounted` 必须 init（不依赖 page transition）
- 启用 page transition 时：路由切换 `destroy()`，`onAfterEnter` 重 init
- `destroy()` 仅 revert 自有 `gsap.context()`，禁止 `ScrollTrigger.getAll().kill()`
- DOM 变更后 `update()`；子组件 inject `scrollInjectionKey` 或 `useLayoutInvalidation()`

## GSAP

- 时间轴工厂放 `lib/scroll/animation.ts`
- 页面挂载用 `useGsapTimeline`
- GSAP ticker 与 Lenis 帧同步（非双 RAF）

## 动效组件

| 组件 | 用途 |
|------|------|
| `ScrollReveal` | InView + 可选 `cssProgress`；`speed` 仅例外场景 |
| `MaskReveal` | 逐行遮罩上滑（`data-mask-line` + `.mask-line-wrap`） |
| `PageIntroCurtain` | 开场幕帘；`useGsapTimeline` + `createPageIntroCurtainTimeline` |
| `MarqueeBand` | 无限横向文字带（CSS） |
| `CyclingText` | Hero / 幕帘循环切换文案 |
| `HoverShuffleText` | 导航 / 链接触发逐字跳动（`createHoverShuffle`） |
| `SplitText` | 逐字 DOM 拆分（供 CharReveal / HoverShuffle 消费） |
| `ScrollSection` | Sticky 容器；避免默认 speed |
| `HeroDepthCanvas` | Hero WebGL 深度（`lib/motion/webglCanvas.ts`） |
| `WebGLRevealMask` | 纹理位移遮罩（共享 webgl 层） |

## 微交互 Composables

| Composable | 用途 |
|------------|------|
| `useMagnetic` | 磁性吸附（`MagneticButton`、任意 CTA 包装） |
| `useMouseParallax` | 商品图鼠标微动（`ProductCardMedia`） |

参数默认值见 `docs/VISUAL-DESIGN.md`。

## 性能门控

- `motionCapabilities`：`full | reduced | static`
- `SSR_MOTION_CAPABILITIES`：SSR / 首屏保守值（`webgl: false`），客户端 mount 后刷新
- `<768px`：`parallax: false`；scrub 回退静态布局
- `prefers-reduced-motion` 跳过平滑滚动与幕帘
- `jankGuard` 触发 `forceMotionDegrade`；测试用 `resetMotionDegradeState()`
- 环境变量：`NUXT_PUBLIC_ENABLE_SMOOTH_SCROLL`、`NUXT_PUBLIC_ENABLE_PARALLAX`

## data-scroll 属性（优先级）

| 属性 | 用途 |
|------|------|
| `data-scroll-css-progress` | **首选** — Hero、Dynasty、scrub |
| `data-scroll` | InView 检测 |
| `data-scroll-class` | 进入视口 class（`is-inview`） |
| `data-scroll-speed` | 例外视差；首页不用 |
| `data-scroll-sticky` | Sticky 吸附 |

## 性能目标

- 避免多层 speed 视差 DOM
- 避免 scale 动画导致 CLS
- `LazyImage` defer 使用 `LAZY_LOAD_ROOT_MARGIN`（15%）
- LCP < 2.5s · CLS < 0.1 · FPS ≥ 60

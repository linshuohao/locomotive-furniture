---
description: Vitest 单元测试与 Playwright E2E 规范
globs: "**/*.{spec,test}.ts"
alwaysApply: false
---

# 测试规范

## 分层

| 目录 | 工具 | 覆盖范围 |
| ---- | ---- | -------- |
| `app/tests/*.spec.ts` | Vitest + jsdom | cart、commerce、products、storage、motionCapabilities、jankGuard、translateError |
| `e2e/*.spec.ts` | Playwright | 电商主流程、checkout 守卫、locale 路由、scroll 首屏 init |

- 单元测试不测试纯 UI 渲染
- E2E 使用 `data-testid`（`add-to-cart`、`cart-checkout`、`place-order`、`locale-switcher`）
- E2E 构建用 `npm run build:e2e`（mock commerce + 关闭动效）

## Vitest

- `globals: true`，环境 `jsdom`
- 配置：`vitest.config.ts`

## Pinia 测试

```ts
beforeEach(() => {
  setActivePinia(createPinia())
  localStorage.clear()
})
```

## Commerce 测试

```ts
import { resetCommerceProvider } from '@/data/providers'

beforeEach(() => {
  resetCommerceProvider()
})
```

## Playwright

```bash
npm run e2e:install   # 首次
npm run e2e           # 全量
npm run e2e:ui        # 调试
```

推送前：`npm run check`（不含 E2E）；CI 额外执行 Playwright。
